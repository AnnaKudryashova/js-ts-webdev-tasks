import { AppPageHalloween } from './pages/AppPageHalloween';
import { heroData, galleryData, footerData } from './data';

document.addEventListener('DOMContentLoaded', () => {
  AppPageHalloween(heroData, galleryData, footerData);
});
